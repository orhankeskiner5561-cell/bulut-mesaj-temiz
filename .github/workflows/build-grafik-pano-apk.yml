name: Build Grafik Pano APK

on:
  push:
    branches:
      - trading-panel-apk-v1
    paths:
      - 'android-trading-panel/**'
      - '.github/workflows/build-grafik-pano-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK packages
        shell: bash
        run: |
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$SDKMANAGER" ]; then
            SDKMANAGER=$(find "$ANDROID_HOME/cmdline-tools" -type f -name sdkmanager | head -n 1)
          fi
          yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
          "$SDKMANAGER" "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Apply V5 live Binance terminal
        working-directory: android-trading-panel
        run: python3 v5_live_patch.py

      - name: Apply V6 UI interaction patch
        working-directory: android-trading-panel
        run: python3 v6_ui_patch.py

      - name: Apply V7 continuous chart pan patch
        working-directory: android-trading-panel
        run: python3 v7_pan_patch.py

      - name: Build debug APK
        working-directory: android-trading-panel
        run: gradle :app:assembleDebug --stacktrace

      - name: Rename APK
        run: cp android-trading-panel/app/build/outputs/apk/debug/app-debug.apk Grafik-Pano-Live-V7.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Grafik-Pano-Live-V7-APK
          path: Grafik-Pano-Live-V7.apk
          if-no-files-found: error